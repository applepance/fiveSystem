<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.five.testsys.mapper.UserCheckMapper">
    <!-- 在select标签中编写查询的SQL语句，设置select标签的id属性为getUser,id属性必须唯一的，不能重复使用
    parameterType属性指名查询时使用的参数类型，resultType属性指明查询返回的结果集类型 -->

    <select id="getUserCheck_ass" parameterType="int" resultType="cn.five.testsys.entity.UserCheck">
        SELECT uc_id,ass_id,leader_id FROM userchecks WHERE ass_id=#{ass_id} ;
    </select>
    
    <select id="getUserCheck_leader" parameterType="int" resultType="cn.five.testsys.entity.UserCheck">
        SELECT uc_id,ass_id,leader_id FROM userchecks WHERE leader_id=#{leader_id} ;
    </select>

</mapper>